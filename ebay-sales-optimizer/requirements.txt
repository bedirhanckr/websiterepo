# eBay Sales Optimizer — pinned to compatible minimums. Python 3.12+ recommended
# (works on 3.11). Deliberately minimal: no Redis, no queue, no web framework.
httpx>=0.27,<1.0
SQLAlchemy>=2.0,<2.1
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0
streamlit>=1.33
pandas>=2.2            # used only by the Streamlit dashboard for table display

# Dev / test
pytest>=8.0
respx>=0.21           # httpx request mocking for API-parser tests
