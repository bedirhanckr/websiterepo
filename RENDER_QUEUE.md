# Render Queue

_Only list items where a new render/photo would significantly improve the portfolio. Assume Bedirhan has roughly one working day available for any new rendering work. Reuse existing assets whenever possible — default to Keep/existing unless there's a real gap._

Nothing queued yet. This file will be populated as each project's asset curation reveals real gaps (e.g., a project with no usable hero image at all). Entries should use this format:

```
## [Project] — [what's missing]
- Reason: why existing assets don't cover this
- Suggested angle:
- Material:
- Lighting:
- Background:
- Priority: High / Medium / Low
```
