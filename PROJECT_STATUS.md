# Project Status

_Last updated: 2026-07-11. Deadline: August 10._

**⚠️ Machine transition: Bedirhan moved to the Germany PC 2026-07-11 — read `HANDOFF-GERMANY.md` first on the new machine.**

## Done

- Hosting live at cakiroglu.netlify.app (Netlify Drop, manual redeploy). **Netlify ZIP fixed 2026-07-09**: `ZipFile.CreateFromDirectory` produced backslash entry names Linux couldn't serve (site loaded unstyled); rebuilt with `System.IO.Compression.ZipArchive` + manual `Replace('\','/')`, staged from `Desktop\portfolio-stage\` (production assets only, ~130 MB vs 15.5 GB full archive) → `Desktop\netlify-linux.zip`.
- Local dev server: PowerShell static server. **Port now 8021** (8010 then 8020 got reserved by Windows) — `files/.claude/launch.json` updated. `serve.ps1` now serves `index.html` for directory URLs (e.g. `/pdf/`).
- TUS Metallbau job title corrected to "Industrial Designer / Ind. Design Engineer" (DE/TR equivalents) in `i18n.js` — both `home.ind.role` and `about.tus.role` keys (about page had its own key, easy to miss).
- All 9 case studies built (Pioneer Mk2, Spiegelau, Razor, Urn, Herptile, Rockhounder, Delta, Retroscent, REX) with verified real images/awards, no invented facts.
- **Portfolio curation (2026-07-09):** Reduced to 5 flagship case studies (Pioneer Mk2, Rockhounder, REX, Delta, Razor). Herptile, Urn, Retroscent, Spiegelau moved to compact "Selected Works" grid (1 image + 1 line each). Home page h2 updated, new `.selected-works-grid` CSS component added.
- **TUS Metallbau industry experience section (2026-07-09):** Added to home page between principles and work grid. 2-column layout (role/company/date + 4 contribution bullets). Natural note: "Representative commercial projects are not included in this portfolio." Full EN/DE/TR i18n.
- **CSS polish (2026-07-09):** `.case-section .eyebrow` margin 8px → 16px; h2 `line-height` added; case section `p`/`li` line-height 1.7; `.media-slot`/`.media-grid` top margin consistent; `.industry-exp` component.
- **Visual language guide:** `RENDER_STYLE.md` created — camera, lighting, materials, background, composition, aspect ratios, thumbnail spec per project. Ready for render regeneration.
- **Sketch section fixes:** Pioneer Mk2 placeholder `.media-slot` removed (honest text disclosure only). Delta sketch section missing h2 added. Both `bodies/` and `work/` files synced.
- Full EN/DE/TR translation of all 9 case studies verified. About page synced with CV.
- **PDF/slide-deck portfolio skeleton complete (2026-07-10)** at `pdf/index.html` — **53 slides**: Cover, CV, Contents (numbered 01–06 project list with thumbnails), 5 flagships × 6 pages each (cover → sketch/ideation slot → 2 detail pages → additional-detail slot → result), Other Selected Work section (20 pages: divider, mini projects at 3 pages each, awards summary). Sketch + "Additional Detail" pages are **dashed-border placeholders** waiting for Bedirhan's scans/renders. See MEMORY.md "PDF deck" for structure conventions (JS auto-numbering, print CSS, etc.).

- **Project description corrections (2026-07-10):** 5 project descriptions corrected in i18n.js (all 3 languages) and pdf/index.html: Spiegelau → "White Wine Set Design" (not champagne), Retroscent → "Furniture for 2050 World Scenarios / aromatherapy" (not just seating), Delta → "Convertible Furniture for Space-Constrained Users" (not kinematic mechanism), Rockhounder → "Product + App for Recreational Rock Collecting / specialist bag system + inner tool bag + ITÜ 2nd Best Graduation Project", Pioneer → "MKII" everywhere (was "Mk2" on website). All HTML page titles/metas also updated.

- **Placeholder fill from archive (2026-07-10 overnight):** 8 of 15 placeholder slides filled with verified archive material (every image visually checked before use): Razor sketch (`razor/sketch-ideation.jpg`, copied from Project IV w3), Herptile sketch+extra (ideation-sketches, exploded+journey-map), Urn sketch+extra (sketch-tea-station, production-details BOM board), Spiegelau sketch+extra (ideation-01, technical-dims), Retroscent extra (2 detail renders), Kickboard sketch (`Kickboard projesi/w2.2/1.jpg` concept sheet), Automotive extra (Evoque interior packaging pair), Pioneer extra (2 pool photos), Delta extra (work-mode render + exhibition photo), REX extra (2 assembly-catalog pages copied to `rex-tank/catalog-page-*.jpg`, shown with new `.fit-contain` class). Deck QA passed: 0 broken images, 0 overflowing slides.

- **Page interlinking + online-view option (2026-07-11, fourth pass):** (a) Added a **prev/next project pager** to all 9 case-study pages (new `.case-pager` component, inserted before `</main>` via `scratchpad/insert-pager.ps1`). Linear chain in homepage order (Pioneer MKII → Rockhounder → REX → Delta → Razor → Herptile → Urn → Retroscent → Spiegelau) with wrap-around (Spiegelau→Pioneer), plus a centered "All work" link back to `index.html#work`. Project names are static (proper nouns); direction labels are i18n (`cs.pager.prev/next/all`, all 3 langs). Verified: chain + wrap-around correct, EN/DE/TR labels, no mobile overflow (2-col at ≤640px with "All work" full-width). (b) Added a **"View online"** option to the homepage Portfolio section (3rd button, held `is-disabled`/coming-soon like the PDF; new key `home.portfolio.online`, note reworded to "Online view and PDF coming soon."). **When the deck is finished:** point "View online" at `pdf/index.html` (the in-browser deck) and "Download PDF" at the exported PDF, removing `is-disabled` from each. Zip rebuilt via rebuild-stage.ps1 (99 entries, 57 MB), pager + keys verified inside.
- **Homepage copy overhaul (2026-07-11, third pass):** Per Bedirhan's feedback the hero was too ad-like — rewrote `home.h1`/`home.lede` shorter and warmer, all 3 languages. The 4 principle blocks had their first-person "I" removed and were made impersonal (`I don't hide constraints` → `Constraints stay visible`, `I think in systems` → `Designed as a system`, `I look for verifiable results` → `Verifiable results`; em-dash clause-connectors replaced with periods per the house rule). Added a **"How I work" header** to the principles section (new `.how-head` with a mono process line "Market research → idea generation → sketching → CAD → engineering → prototyping → verification" and a software line "Fusion 360 · SolidWorks · Rhino · HiCAD · KeyShot · Blender · Photoshop"; new keys `home.how.eyebrow/process/tools`). Restructured CSS: `.principles .wrap` grid → `.principle-grid`. Dropped the "Selected Work" descriptive h2 (`home.work.h2` no longer used; eyebrow alone now). Reworded `footer.cta` to "Have something you want to build? Let's talk." (warmer, invitational). **New Portfolio section** (`#portfolio`, after Selected Works): eyebrow + h2 + copy, an active **View on Behance** button (behance.net/bedirhanckr) and a **Download PDF** button held in a disabled/`is-disabled` state with a "PDF coming soon" note — swap to `<a href="…pdf" class="btn">` once the PDF is uploaded. New keys `home.portfolio.*` (6). All verified on the dev server: `window.I18N` loads, EN/DE/TR switch cleanly, 4-col grid intact, no horizontal scroll on mobile (375px), 0 broken images.
- **⚠️ portfolio-stage vanished & was rebuilt clean (2026-07-11):** the `Desktop\portfolio-stage\` folder disappeared mid-session (cause unknown — not deleted by the session, which only copied *into* it). Rebuilt it reproducibly from portfolio-hosted with the new **`rebuild-stage.ps1`** (saved at repo root): it copies the served core files (index/about/robots/sitemap, work/*.html, assets/css+js, pdf/index.html) plus only the assets those files actually reference, then builds `netlify-linux.zip` with forward-slash entries. Result: **99 files / 62 MB stage, 99-entry / 57 MB zip** — leaner than the old 141-file/147 MB stage, which had ~42 unreferenced leftover images. Verified every asset reference resolves and no internal docs leak into the zip. **From now on, don't hand-copy into staging — run `rebuild-stage.ps1`.**
- **Content trim + critical i18n fix (2026-07-11, second pass):** 7 overlong paragraphs trimmed ~25-30% in all 3 languages (Herptile: problem/arastirma/konsept/kazanim; Urn: problem/dfm/kazanim) — applied to i18n.js EN/DE/TR + the hardcoded TR defaults in `work/` and `bodies/` (`cs.urn.arastirma.p` deliberately skipped, it contains escaped quotes). **Found and fixed a site-breaking bug:** the 2026-07-10 Delta description correction left an unescaped straight quote in `cs.delta.problem.p` (DE) — `„Multifunktions"-Lösungen` — which was a JS syntax error, so `window.I18N` never loaded and **language switching was dead site-wide since 2026-07-10** (pages fell back to hardcoded TR). Fixed with a proper German closing quote (`“`). Caught before deploy; the live 2026-07-09 site was never affected. Zip rebuilt same size (129 entries, 139 MB), fixed i18n.js verified inside the zip. Lesson: **after editing i18n.js, always load a page and check `typeof window.I18N === 'object'`** — a single unescaped `"` in any string kills all translations silently (page still renders via TR defaults).
- **Pre-departure pass (2026-07-11):** staging (`Desktop\portfolio-stage\`) synced with all 2026-07-10 corrections (i18n.js, index.html, work/pioneer-mk2/retroscent/spiegelau, pdf/index.html) + the 13 archive images the PDF deck references that weren't staged yet. Case-study section padding bumped 64px → 96px desktop (PRIORITY item "more white space", kept 64px ≤720px) — verified on dev server, 96px computed, 0 broken images; deck re-verified 53 slides / 0 broken / 7 placeholders. **`netlify-linux.zip` rebuilt (129 entries, 139 MB, forward-slash entries verified)** — now **excludes internal docs** (MEMORY/PRIORITY/PROJECT_STATUS/KARAR-GUNLUGU/RENDER_*/CLAUDE.md, build.py, serve.ps1, bodies/, inbox/, stale CV-*.pdf, pdf/deck-v2.html) which were publicly accessible in the previous deploy. Nothing links to the CV PDFs or deck-v2 (grep-verified), so removal is safe.

## In Progress / Next Up

1. **Netlify deploy (manual, Bedirhan)**: drag `Desktop\netlify-linux.zip` (2026-07-11 build) onto app.netlify.com → Deploys. Live site is still the 2026-07-09 version without the description corrections and with internal docs exposed.
2. **Waiting on Bedirhan**: sketch scans / re-renders for the **7 remaining empty slots**: Pioneer sketch, Rockhounder sketch + extra, REX sketch, Delta sketch, Razor extra, Retroscent sketch. Drop into `assets/img/<project>/`, then tell the session the paths. (Not uploaded before departure; will happen from Germany.)
3. After content lands: final visual pass over all 53 slides, then export PDF via Ctrl+P (print CSS is in place — margins None, background graphics ON), then rebuild + redeploy the zip (stage the new images too).
4. **Homepage Portfolio section — finish the three pending links:** (a) upload projects to Behance (profile behance.net/bedirhanckr exists but is currently empty); (b) turn the disabled **"View online"** button into `<a href="pdf/index.html">` once the deck's 7 placeholders are filled (it's the in-browser viewable version); (c) once the PDF deck is exported, drop it somewhere linkable (e.g. `portfolio-hosted/Bedirhan-Cakiroglu-Portfolio.pdf`) and turn the disabled **"Download PDF"** button into a real `<a href>`. Then reword/remove the "Online view and PDF coming soon" note.
5. **bodies/home.html is stale** — it predates the 5-flagship curation (still shows 9 projects, no Selected Works / industry / Portfolio sections) and is NOT the served file. `index.html` is authoritative and hand-edited. Either reconcile bodies/home.html later or leave it; build.py is broken regardless so it doesn't regenerate.
4. **Next web pass**: hero render regeneration using `RENDER_STYLE.md` (Bedirhan supplies renders; guide is ready).
5. "Other Work" gallery (Automotive Design sketches) — queued after render pass.
6. Global hiring-manager critique + "How I Work" capabilities section redesign — queued last.
7. **If the Germany PC has Python/Node**: unblocks standalone CV PDF fix (stale Alias/CATIA skills) and `build.py` repair — check on first session there.

## Blocked / Waiting on Bedirhan

- TUS Metallbau real projects: needs company URL (do not guess) + a future Germany trip for CAD files.
- Sim Racing Steering Wheel, 3D Printing product: not in archive, will be uploaded separately.
- Ground Control Box render for Pioneer addendum.
- Standalone CV PDFs (CV-en/de/tr-...pdf) still have the old Alias/CATIA skills list — this machine has no Python so I can't safely text-edit the PDF in place; Bedirhan should fix at the source or wait for a regenerated PDF from the new HTML deck.
- Confirm which project's "pafta" (technical plate) images were made with AI/Photoshop rather than real CAD output — assumed Retroscent, softened that page's DFM claim accordingly, unconfirmed.

## Not blocking, lower priority

- `build.py` has broken Linux paths — manual sync of `bodies/` + built output works fine, low priority to fix.
- `robots.txt` is `Disallow: /` until the site is finished — flip once the visual pass + gallery are done.
- Print/PDF CSS for site-native PDF export — superseded for now by the dedicated slide-deck approach.
