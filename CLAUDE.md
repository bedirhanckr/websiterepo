# Start here

Read `PROJECT_STATUS.md`, `PRIORITY.md`, then `MEMORY.md` in this folder, in that order, before doing anything else — they're kept current and compressed after every major task.

If the session was launched from `C:\Users\MONSTER\Desktop\files\` instead of here, that folder has its own `CLAUDE.md` with the dev-server/tooling setup (port, preview_screenshot caveat, Artifact CSP caveat) — read that one too, it isn't duplicated here.

Deadline: August 10.
